---
description: Show claude-card daemon + device status.
---

Shows daemon process state, device pair-status, current widget cache, and
last firmware boot line.

!`bash "$CLAUDE_PLUGIN_ROOT/scripts/status.sh"`
